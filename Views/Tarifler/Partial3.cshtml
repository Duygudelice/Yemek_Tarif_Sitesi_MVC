@using YemekTarif.Models.sınıflar
@model List<Yorumlar>
<div class="comments">
    <h3>Son Yorumlar</h3>
    <ul>
        @foreach (var x in Model)
        {
            <li><a href="#">@x.AdSoyad </a> on <a href="/Tarifler/TarifDetay/@x.ID">@x.Yemek.Ad</a></li>
        }
    </ul>
</div>
